server:
  port: 8082
spring:
  cloud:
    config:
      server:
        git:
          default-label: gateway
          uri: https://github.com/emanuelquinto/Microservicios-Apis
          search-paths: config-data
  application:
    name: Config-Server
          
            
        